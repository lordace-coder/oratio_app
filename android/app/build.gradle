plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
}

android {
    namespace = "com.acemedia.oratio"
    compileSdk = 36
    ndkVersion = "29.0.13599879"
    
    compileOptions {
        // ✅ Match Java with Kotlin: both target Java 17
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    
    kotlinOptions {
        jvmTarget = '17'
    }
    
    defaultConfig {
        applicationId = "com.acemedia.oratio"
        minSdk = 25
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
        proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
    }
    
    signingConfigs {
        release {
            keyAlias 'cathsapp'
            keyPassword 'lordace12'
            storeFile file('my-release-key.keystore')
            storePassword 'lordace12'
        }
    }
    
    buildTypes {
        debug {
            // Debug configuration - no signing needed
            debuggable true
            minifyEnabled false
            shrinkResources false
            // Use default debug keystore (auto-generated)
        }
        release {
            // Release configuration with your signing
            signingConfig signingConfigs.release
            debuggable false
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}